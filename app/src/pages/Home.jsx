import GlobalLayout from "../layouts/GlobalLayout";
import { Typography } from "@mui/material";

const Home = () => {
    return(
        <GlobalLayout title="Tilaa">
            <Typography variant="caption">TODO</Typography>
        </GlobalLayout>
    )
};

export default Home;